apiVersion: v2
name: acs-declarative-config
version: 0.1.0
description: Chart generates declarative config for workshop to limit users to specific namespaces and permissions
type: application
kubeVersion: '>=1.14.6'
sources:
 - https://github.com/OpenShiftDemos/
maintainers:
- name: Gerald Nunn
